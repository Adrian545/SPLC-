using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ift : MonoBehaviour
{
    private int poin;
    // Start is called before the first frame update
    void Start()
    {
        if (ifno.puncte > 0)
            ifno.puncte = 0;
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
